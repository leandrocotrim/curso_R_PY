# vector of values
pesos = c(10, 20, 40, 60, 80, 80, 100, 20)

# show length
length(pesos)

# show mean
mean(pesos)

# show median
median(pesos)

# show quantiles
quantile(pesos)

# show stand desviation
sd(pesos)

# show max
max(pesos)

# show min
min(pesos)

# summary
summary(pesos)

# moda
table(pesos)

# table.pesos.max = max(table.pesos)
# df.pesos = as.data.frame.table(table.pesos)
# df.rows = df.pesos$Freq == table.pesos.max
# as.vector(df.pesos[df.rows,]$pesos)

